
1. Start the server in the test environment
    rails server -e test

2. migrate and seed the test enviroment (ONLY ONCE)
    rake environment RAILS_ENV=test db:migrate
    rake environment RAILS_ENV=test db:seed

3. Now you can run the spec
    rake spec