<table class="table table-striped">
  <thead>
    <tr>
      <th>Title</th>
      <th>Category</th>
      <th>Content</th>
    </tr>
  </thead>
  <tbody>
  <% @tips.each do |tip| %>
  <tr>
    <td><%= link_to tip.title, admin_tip_path(tip)%></td>
    <td>
      <% tip.categories.each_index do |i|%>
	        <%= tip.categories[i].name %><% if i + 1 != tip.categories.length %>,<% end %>
      <% end %>
    </td>
    <td><%=tip.content%></td>
  </tr>
  <% end %>
</table>

<a href="/admin/tips/new">
  <button class="btn">
    New Tip
  </button>
</a>
